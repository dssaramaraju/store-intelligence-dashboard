services:
  api:
    build: .
    ports:
      - "8000:8000"
    environment:
      STORE_DB_PATH: /app/data/store_intelligence.db
    volumes:
      - ./data:/data:ro
      - api-data:/app/data

volumes:
  api-data:
